<% label ||= nil %>
<% note  ||= nil %>

<div>
  <%= form.submit (label || 'Save'),
                  class: 'mt-3 mb-5 btn btn-primary btn-lg' %>

  <% if note.present? %>
    <p class='form-text mb-3'>
      <%= note %>
    </p>
  <% end %>
</div>
