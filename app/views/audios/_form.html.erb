<%= form_with(model: audio) do |form| %>
  <% if audio.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(audio.errors.count, "error") %> prohibited this audio from being saved:</h2>

      <ul>
        <% audio.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :url, style: "display: block" %>
    <%= form.text_area :url %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.label :embed, style: "display: block" %>
    <%= form.check_box :embed %>
  </div>

  <div>
    <%= form.label :template, style: "display: block" %>
    <%= form.text_field :template %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
